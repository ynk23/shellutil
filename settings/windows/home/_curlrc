# Definition curl defoult option.
# This file is for curl in windows os.
# Windows os usually load ~/_curlrc file instead of ~/.netrc file.
# Similarly load ~/_netrc file instead of ~/.netrc file.

# follows a redirect and the request again.
-L
# Using ~/_netrc
--netrc
